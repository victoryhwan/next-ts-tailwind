# 웹브라우저에서 javascript

자바스크립트는 `싱글스레드` 이다. 

### 동작원리
- javascript 코드(함수)를 스택에 넣고 하나씩 실행한다.
- 스택은 하나만 존재, 그래서 한번에 코드 하나만 실행 가능
    - 크롬의 경우 동시성 문제를 일으킬 수 있다는 이유로 단일 웹 사이트 페이지의 자바스크립트 코드가 동시에 실행되는 것을 허용하지 않는다.
- 콜백은 큐에 넣고 기다리다가 스택이 비어있으면 스택으로 보내준다


